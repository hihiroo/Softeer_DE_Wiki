# 리뷰
## W3M1
* Docker 컨테이너 하나에서 하둡을 직접 설치하고 실행해봄
    * Dockerfile부터 작성하지 않고, 콘솔에서 하나씩 검증한 뒤, 실제로 성공했던 명령어만 정리해 Dockerfile로 옮김

## W3M2a
* 싱글 노드에서 구성했던 환경을 확장하여 master, worker 역할 분리
* Docker 네트워크를 사용해 컨테이너 간 통신 구성
    * 구체적인 원리 더 공부 필요
* MapReduce 작업을 YARN 기반으로 실행하면서 `MRAppMaster ClassNotFoundException` 오류가 떠서 mapred-site.xml에 환경 변수 설정 추가해 오류 해결함

## W3M2b
* modify 스크립트 구현 완료
    * 파이썬 + json 구조로 구현
        * xml 파일 파싱 및 작성을 위해 쉘 스크립트와 파이썬 중 어떤 언어를 쓸지 고민하다가 파이썬 선택
            * 챗지피티에 물어본 결과, xml은 계층 구조로 되어 있고, 중복 태그가 있기 때문에 sed나 awk로 처리하기 어렵다고 함
        * 요구사항에서 주어진 key, value를 코드에 넣지 않기 위해 json 파일로 분리

# 회고

## Keep
* 과제에 선택사항이 있는 경우 어떤 방법이 더 적합한지 챗지피티에 검색해보고 선택하기

## Problem
* 집중력이 깨질 때마다 믹스커피를 1잔씩 마셨더니 3잔 이후부터 속이 불편했음
    * 믹스 커피는 하루 최대 2잔만 마시기

## Try
* 집중력이 깨지면 1차 스트레칭, 그래도 집중이 안되면 양치해보기